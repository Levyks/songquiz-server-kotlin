package com.levyks.songquiz.enums

enum class RoomStatus {
    IN_LOBBY,
    IN_GAME,
    RESULTS,
}

enum class RoundStatus {
    PLAYING,
    RESULTS,
}

enum class RoundType {
    SONG,
    ARTIST,
}